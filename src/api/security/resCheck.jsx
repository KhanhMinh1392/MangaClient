

export const errCodeRes = (errorResponse) => {
    switch (errorResponse.status) {
        case 400:
            // switch (errorResponse.data.content.error) {
            //     case 
            // }
    }
}